(function () {
  var commentHtml = "<%= j render('searches/stats', :array => @array, :name => @name, :title => @title, :blurb => @blurb, :image => @image) %>";
  $('.ajax').empty()
  $('.ajax').append(commentHtml);
  console.log("its working!")
})();

